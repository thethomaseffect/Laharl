///<reference path="../typings/backbone/backbone.d.ts"/>

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var GamesRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return GamesRouter;
});
